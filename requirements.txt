# InstaAgent — Python 3.11+ dependencies for Repost Pipeline

# YouTube Data API v3 (OAuth2 upload)
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1

# Image processing validation
Pillow>=10.4.0

# Meta Graph API HTTP clients
requests==2.31.0

# Config & environment
python-dotenv==1.0.1
PyYAML==6.0.1

# Logging color (optional, used in logger.py)
colorlog==6.8.2

# Cloudinary for cloud image hosting
cloudinary==1.39.0

# Instagram scraping for Repost mode
instaloader==4.13.2

# Excel tracker (replaces reposted_ids.txt + last_post_type.txt)
openpyxl>=3.1.0
